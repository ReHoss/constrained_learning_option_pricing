"""Tests for exact terminal / exercise-date functions (g1, g2).

Key properties to verify:
    - g2(s, T) == Φ(s) for all s  (terminal condition exactly satisfied)
    - g1(s, T) == 0                (vanishes at terminal)
    - g2 matches the Black-Scholes formula for European options
    - g2 captures the √τ singularity near T (compare against reference)
"""

# TODO: implement tests
